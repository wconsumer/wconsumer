language: php

env:
  global:
    secure: "P1XQmn/kCeN0nWypHiwK+5HlRys5LLF3UVdksMjnt3olIiZWmSx4MzPHlKirzcVD0X8DMSEBIphS3cLwLTuEJyCx9XpSsypX9PfRyeZmTRcAnXklkV61k8fvoEEbb18QCtwLIhiGzXVd2JaWAwSod/mmOHU2ns2+ZsKeMb0TOC4="

php:
  - 5.3

mysql:
  database: drupal
  username: root
  encoding: utf8

before_script:
  - curl -s http://getcomposer.org/installer | php
  - php composer.phar install --prefer-source --no-interaction --dev
  - mysql -e 'create database drupal;'
  - pear channel-discover pear.drush.org
  - pear install drush/drush
  - phpenv rehash
  - cd ..
  - drush dl drupal --drupal-project-rename=drupal
  - cd drupal
  - drush site-install --db-url=mysql://root:@127.0.0.1/drupal --yes
  - mv ../wconsumer ./sites/all/modules/
  - drush en --yes wconsumer
  - cd sites/all/modules/wconsumer/
  - openssl aes-256-cbc -k "$TEST_KEYS_SECRET" -in integration-tests/keys.php.enc -d -a -out integration-tests/keys.php
  - sh acceptance-tests/start-selenium.sh

script:
  - phpunit
  - cd integration-tests && phpunit && cd -
  - cd acceptance-tests && phpunit && cd -
  - php merge-coverage-reports.php

after_script:
  - sh acceptance-tests/output-screenshots.sh